server:
  port: 8888
cors:
  originPatterns: http://localhost:8080,http://localhost:3000
security:
  jwt:
    secret-key: xlAK8ReAUGUK2GDMlZwcKQ4uvLI2zUeHoM9S5R9rZHY=
    expiration: 3600000 # 1 hora
  password:
    encoder:
      secret: Vy12E+9yHPdtB0aCfQmUUxgkwPJy3yzGpHTCUS2iClg=
      iterations: 185000 # 185.000 iterações
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    enabled: true
    locations: classpath:db/migration
logging:
  level:
    root: info
spring-doc:
  paths-to-match: /api/v1/**/**
  swagger-ui:
    use-root-path: true